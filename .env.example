# Base URL of your FastAPI backend (Phase 6).
# On a real device via Expo Go, "localhost" means the phone itself —
# use your PC's LAN IP instead, e.g. http://192.168.1.42:8000
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
